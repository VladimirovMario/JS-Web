import { AppMaxCountDirective } from './custom-form-validator.directive';

describe('CustomFormValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new AppMaxCountDirective();
    expect(directive).toBeTruthy();
  });
});
